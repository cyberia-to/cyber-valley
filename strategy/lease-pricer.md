---
tags: cyberia, cyber valley, strategy, tool
alias: ground lease pricer, lease calculator, lease pricer
crystal-type: pattern
crystal-domain: cyberia
---
# lease pricer

interactive pricer for ground-lease deals: leasehold economics = relativity × freehold; whatever the premium leaves on the table returns as rent at a risk-adjusted rate. the review clause pulls rent back to X% of current land value when land outruns the index — the protection [[development]] instrument B needs when g > CPI. long-term value preservation of the rent stream itself is the [[century index]].

<div id="lcalc"></div>
<style> #lcalc{--bg:#000;--s1:#0a0a0a;--s2:#111;--ln:#222;--tx:#f0f0f0;--mut:#8b948c;--neon:#22c55e;--cyan:#06b6d4;--red:#ef4444;background:var(--bg);color:var(--tx);font-family:var(--font-body,'Play',system-ui,sans-serif);border:1px solid var(--ln);border-radius:12px;padding:20px;margin:16px 0;box-shadow:inset 0 0 80px rgba(34,197,94,.05)} #lcalc .cols{display:flex;flex-direction:column;gap:14px} #lcalc .panel{background:var(--s1);border:1px solid var(--ln);border-radius:10px;padding:16px} #lcalc .ctrl{display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:12px 24px} #lcalc .out{display:flex;flex-direction:column;gap:14px} #lcalc h4{grid-column:1/-1;font-family:var(--font-mono,'JetBrains Mono',monospace);font-size:11px;color:var(--neon);letter-spacing:2.5px;text-transform:uppercase;margin:8px 0 0;text-shadow:0 0 10px rgba(34,197,94,.55)} #lcalc h4:first-child{margin-top:0} #lcalc .row{margin:0} #lcalc .row .top{display:flex;justify-content:space-between;align-items:baseline;margin-bottom:4px} #lcalc label{font-size:12.5px;color:var(--mut)} #lcalc .val{font-family:var(--font-mono,'JetBrains Mono',monospace);font-size:13px;color:var(--tx)} #lcalc input[type=range]{width:100%;accent-color:var(--neon);height:3px;cursor:pointer;margin:0} #lcalc .stats{display:flex;gap:12px;flex-wrap:wrap} #lcalc .stat{background:var(--s2);border:1px solid var(--ln);border-radius:8px;padding:11px 14px;flex:1;min-width:140px} #lcalc .stat .l{font-size:11px;color:var(--mut);margin-bottom:4px;letter-spacing:.4px} #lcalc .stat .v{font-family:var(--font-mono,'JetBrains Mono',monospace);font-size:19px;font-weight:600;text-shadow:0 0 12px rgba(34,197,94,.25)} #lcalc .stat .s{font-size:10.5px;color:var(--mut);margin-top:3px} #lcalc .note{font-size:12px;color:var(--mut);line-height:1.6;margin:10px 0 0} #lcalc svg text{font-family:var(--font-mono,'JetBrains Mono',monospace);font-size:10px;fill:var(--mut)} #lcalc .legend{font-size:11px;color:var(--mut);display:flex;gap:16px;margin:6px 0 0 8px;flex-wrap:wrap} #lcalc .legend i{display:inline-block;width:14px;height:2.5px;vertical-align:middle;margin-right:5px} </style>
<script> (function(){ const root=document.getElementById('lcalc'); const P={FH:100000,T:30,k:87,p:20,rBase:9,spread:5,cpi:4,g:10,N:5}; const fmt=v=>v>=1000?'$'+(v/1000).toFixed(v>=100000?0:1)+'k':'$'+Math.round(v); const full=v=>'$'+Math.round(v).toLocaleString('en-US'); const pct=(v,d=1)=>v.toFixed(d)+'%'; const DEFS=[ ['deal','Сделка'], ['FH','Freehold сегодня',20000,2000000,10000,'$',''], ['T','Срок',5,99,1,' лет',''], ['k','Relativity k',50,99,1,'%','Доля freehold за срок пользования: 30л ≈ 87%, 50л ≈ 95%'], ['p','Премия (доля экономики)',0,100,5,'%','Ниже премия → больше кэша бизнесу, выше ставка на ренту'], ['rates','Ставки'], ['rBase','Базовая ставка (100% премия)',5,18,0.5,'%',''], ['spread','Риск-спред (при 0% премии)',0,10,0.5,'%','Риск дефолта растёт при малой премии'], ['cpi','Индекс (CPI)',0,15,0.5,'%',''], ['grow','Защита от роста земли'], ['g','Рост земли g',0,20,0.5,'%','Бали исторически обгоняет CPI'], ['N','Review каждые',1,15,1,' лет','Рента = max(индексированная, X% × рыночный freehold на дату review); X = стартовый yield — та же позиция по земле']]; let html=''; for(const d of DEFS){ if(d.length===2){html+='<h4>'+d[1]+'</h4>';continue} const[id,lab,mn,mx,st,suf,hint]=d; html+='<div class=row'+(hint?' title="'+hint+'"':'')+'><div class=top><label>'+lab+'</label><span class=val id=v_'+id+'></span></div>'+'<input type=range id=in_'+id+' min='+mn+' max='+mx+' step='+st+' value='+P[id]+'>'+'</div>'; } root.innerHTML='<div class=cols><div class="panel ctrl">'+html+'</div>' +'<div class=out><div class=stats id=st1></div>' +'<div class=panel><h4>Траектория ренты, $/год</h4><div id=chart></div>' +'<div class=legend><span><i style="background:var(--mut)"></i>X% × земля (цель)</span>' +'<span><i style="background:var(--red)"></i>только индекс</span>' +'<span><i style="background:var(--neon)"></i>индекс + review</span></div></div>' +'<div class=panel><h4>Что даёт review</h4><div class=stats id=st2></div>' +'<p class=note>Красная линия — рента индексируется, но её доля от актива тает, когда g &gt; CPI. ' +'Зелёная — на каждом review рента подтягивается к X% от текущей стоимости земли и индексируется с новой базы. ' +'При g = CPI линии совпадают: review ничего не стоит арендатору — чистая страховка.</p></div></div></div>'; function calc(){ const pf=P.p/100, rAdj=(P.rBase+P.spread*(1-pf))/100, cpi=P.cpi/100, g=P.g/100; const totalPV=P.k/100*P.FH, premium=pf*totalPV, remainder=totalPV-premium; let A=0; for(let t=1;t<=P.T;t++) A+=Math.pow(1+cpi,t-1)/Math.pow(1+rAdj,t); const rent1=remainder/A, yld=rent1/P.FH*100, X=yld; const rows=[]; let bi=rent1,br=rent1,pvI=0,pvR=0,nomI=0,nomR=0; for(let t=1;t<=P.T;t++){ if(t>1){bi*=1+cpi;br*=1+cpi} const land=P.FH*Math.pow(1+g,t-1), target=X/100*land; if(t>1&&(t-1)%P.N===0) br=Math.max(br,target); rows.push([t,bi,br,target]); pvI+=bi/Math.pow(1+rAdj,t); pvR+=br/Math.pow(1+rAdj,t); nomI+=bi; nomR+=br; } return{rAdj:rAdj*100,premium,totalPV,rent1,yld,rows,pvI,pvR,nomI,nomR, landEnd:P.FH*Math.pow(1+g,P.T-1),takeI:premium+pvI,takeR:premium+pvR}; } function stat(l,v,s,c){return '<div class=stat><div class=l>'+l+'</div><div class=v'+(c?' style=color:'+c:'')+'>'+v+'</div>'+(s?'<div class=s>'+s+'</div>':'')+'</div>'} function chart(rows){ const W=640,H=290,L=54,R=10,Tp=10,B=24,pw=W-L-R,ph=H-Tp-B; const mx=Math.max(...rows.map(r=>Math.max(r[2],r[3])))*1.06; const x=t=>L+pw*(t-1)/Math.max(rows.length-1,1), y=v=>Tp+ph*(1-v/mx); const line=(i,col,dash)=>'<polyline fill=none stroke='+col+' stroke-width='+(i===2?2.2:1.5)+(dash?' stroke-dasharray="3 4"':'') +' points="'+rows.map(r=>x(r[0]).toFixed(1)+','+y(r[i]).toFixed(1)).join(' ')+'"/>'; let ticks=''; for(let i=0;i<=4;i++){const v=mx*i/4,yy=y(v); ticks+='<line x1='+L+' y1='+yy+' x2='+(W-R)+' y2='+yy+' stroke=#1a1a1a stroke-width=0.5></line>' +'<text x='+(L-6)+' y='+(yy+3)+' text-anchor=end>'+fmt(v)+'</text>';} const step=Math.ceil(rows.length/8); for(let t=1;t<=rows.length;t+=step) ticks+='<text x='+x(t)+' y='+(H-6)+' text-anchor=middle>'+t+'</text>'; return '<svg viewBox="0 0 '+W+' '+H+'" style="width:100%;height:auto">'+ticks +line(3,'#555555',1)+line(1,'#ef4444')+line(2,'#22c55e')+'</svg>'; } function render(){ for(const d of DEFS){if(d.length===2)continue;const id=d[0]; document.getElementById('v_'+id).textContent=(d[5]==='$'?full(P[id]):P[id]+d[5]);} const c=calc(), gain=c.takeR-c.takeI; document.getElementById('st1').innerHTML= stat('Премия (первый платёж)',fmt(c.premium),P.p+'% × '+fmt(c.totalPV)+' экономики','var(--neon)') +stat('Рента, год 1',fmt(c.rent1)+'/г','yield '+pct(c.yld,2)+' · ставка '+pct(c.rAdj)) +stat('PV сделки',fmt(c.takeR),'vs '+fmt(c.takeI)+' без review'); document.getElementById('st2').innerHTML= stat('Прирост PV от review','+'+fmt(gain),'+'+pct(gain/c.takeI*100)+' к сделке','var(--cyan)') +stat('Номинал ренты за срок',fmt(c.nomR),'vs '+fmt(c.nomI)+' без review') +stat('Земля в конце',fmt(c.landEnd),'рост '+P.g+'%/год, '+P.T+' лет'); document.getElementById('chart').innerHTML=chart(c.rows); } for(const d of DEFS){if(d.length===2)continue;const id=d[0]; document.getElementById('in_'+id).addEventListener('input',e=>{P[id]=parseFloat(e.target.value);render()});} render(); })(); </script>

the model: leasehold economics = k × freehold; the premium takes p of it upfront, the remainder returns as CPI-indexed rent discounted at r = r_base + spread·(1−p) — a smaller premium shifts value into the rent stream and raises the rate, pricing the tenant's default risk. review every N years resets rent to max(indexed path, X% × market freehold), where X defaults to the initial yield: the lessor keeps the same position in the land forever.
