---
tags: cyberia, cyber valley, strategy, tool
alias: ground lease pricer, lease calculator, lease pricer
crystal-type: pattern
crystal-domain: cyberia
---
# lease pricer

interactive pricer for ground-lease deals. start from the lease value at full prepayment: at least 30% is paid at signing, the rest converts into an annual schedule that compounds three costs — the cost of money (discount), the money index ([[century index]]), and real land growth above it. pricing for [[development]] instrument B.

<div id="lcalc"></div>
<style> #lcalc{--bg:#000;--s1:#0a0a0a;--s2:#111;--ln:#222;--tx:#f0f0f0;--mut:#8b948c;--neon:#22c55e;--cyan:#06b6d4;--red:#ef4444;background:var(--bg);color:var(--tx);font-family:var(--font-body,'Play',system-ui,sans-serif);border:1px solid var(--ln);border-radius:12px;padding:20px;margin:16px 0;box-shadow:inset 0 0 80px rgba(34,197,94,.05)} #lcalc .cols{display:flex;flex-direction:column;gap:14px} #lcalc .panel{background:var(--s1);border:1px solid var(--ln);border-radius:10px;padding:16px} #lcalc .ctrl{display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:12px 24px} #lcalc .out{display:flex;flex-direction:column;gap:14px} #lcalc h4{grid-column:1/-1;font-family:var(--font-mono,'JetBrains Mono',monospace);font-size:11px;color:var(--neon);letter-spacing:2.5px;text-transform:uppercase;margin:8px 0 0;text-shadow:0 0 10px rgba(34,197,94,.55)} #lcalc h4:first-child{margin-top:0} #lcalc .row{margin:0} #lcalc .row .top{display:flex;justify-content:space-between;align-items:baseline;margin-bottom:4px} #lcalc label{font-size:12.5px;color:var(--mut)} #lcalc .val{font-family:var(--font-mono,'JetBrains Mono',monospace);font-size:13px;color:var(--tx)} #lcalc input[type=range]{width:100%;accent-color:var(--neon);height:3px;cursor:pointer;margin:0} #lcalc .stats{display:flex;gap:12px;flex-wrap:wrap} #lcalc .stat{background:var(--s2);border:1px solid var(--ln);border-radius:8px;padding:11px 14px;flex:1;min-width:140px} #lcalc .stat .l{font-size:11px;color:var(--mut);margin-bottom:4px;letter-spacing:.4px} #lcalc .stat .v{font-family:var(--font-mono,'JetBrains Mono',monospace);font-size:19px;font-weight:600;text-shadow:0 0 12px rgba(34,197,94,.25)} #lcalc .stat .s{font-size:10.5px;color:var(--mut);margin-top:3px} #lcalc .hint{font-size:10.5px;color:var(--mut);margin-top:4px;line-height:1.4} #lcalc .note{font-size:12px;color:var(--mut);line-height:1.6;margin:10px 0 0} #lcalc svg text{font-family:var(--font-mono,'JetBrains Mono',monospace);font-size:10px;fill:var(--mut)} #lcalc .legend{font-size:11px;color:var(--mut);display:flex;gap:16px;margin:6px 0 0 8px;flex-wrap:wrap} #lcalc .legend i{display:inline-block;width:14px;height:2.5px;vertical-align:middle;margin-right:5px} </style>
<script>(function(){ const root=document.getElementById('lcalc'); const P={FH:100000,T:25,p:30,rBase:6,cpi:6,g:5}; const fmt=v=>v>=1000?'$'+(v/1000).toFixed(v>=100000?0:1)+'k':'$'+Math.round(v); const full=v=>'$'+Math.round(v).toLocaleString('en-US'); const pct=(v,d=1)=>v.toFixed(d)+'%'; const DEFS=[ ['deal','The deal'], ['FH','Upfront lease value',20000,2000000,10000,'$',''], ['T','Lease term',7,25,1,' yrs','cyber valley standard: 25 yrs + pre-agreed extension options'], ['p','Paid upfront, % of lease value',30,100,5,'%','30% minimum; the rest converts into annual rent'], ['rates','What the schedule compounds'], ['rBase','Cost of money',4,16,0.5,'%','discount rate — the time value of the deferred payments'], ['cpi','Expected inflation',0,15,0.5,'%','drift of the money leg — rent indexes per the <a href=/cyberia/protocol/century-index>century index</a>'], ['g','Land price growth',0,10,0.5,'%','real appreciation above inflation — nominal land growth ≈ inflation + this']]; let html=''; for(const d of DEFS){ if(d.length===2){html+='<h4>'+d[1]+'</h4>';continue} const[id,lab,mn,mx,st,suf,hint]=d; html+='<div class=row><div class=top><label>'+lab+'</label><span class=val id=v_'+id+'></span></div>' +'<input type=range id=in_'+id+' min='+mn+' max='+mx+' step='+st+' value='+P[id]+'>' +(hint?'<div class=hint>'+hint+'</div>':'')+'</div>'; } root.innerHTML='<div class=cols><div class="panel ctrl">'+html+'</div>' +'<div class=out><div class=stats id=st1></div>' +'<div class=panel><h4>Rent trajectory, $/yr</h4><div id=chart></div>' +'<p class=note id=note></p></div></div></div>'; function calc(){ const pf=P.p/100, r=P.rBase/100, i=P.cpi/100, gr=P.g/100; const G=(1+i)*(1+gr)-1; const premium=pf*P.FH, remainder=P.FH-premium; let A=0; for(let t=1;t<=P.T;t++) A+=Math.pow(1+G,t-1)/Math.pow(1+r,t); const rent1=remainder/A, yld=rent1/P.FH*100; const rows=[]; let bf=rent1, nom=0; for(let t=1;t<=P.T;t++){ if(t>1){bf*=1+G;} rows.push([t,bf]); nom+=bf; } return{G:G*100,premium,rent1,yld,rows,nom,last:bf,mvEnd:P.FH*Math.pow(1+G,P.T-1)}; } function stat(l,v,s,c){return '<div class=stat><div class=l>'+l+'</div><div class=v'+(c?' style=color:'+c:'')+'>'+v+'</div>'+(s?'<div class=s>'+s+'</div>':'')+'</div>'} function chart(rows){ const W=640,H=290,L=54,R=10,Tp=10,B=24,pw=W-L-R,ph=H-Tp-B; const mx=Math.max(...rows.map(r=>r[1]))*1.06; const x=t=>L+pw*(t-1)/Math.max(rows.length-1,1), y=v=>Tp+ph*(1-v/mx); const line=(i,col,wd,dash)=>'<polyline fill=none stroke='+col+' stroke-width='+wd+(dash?' stroke-dasharray="3 4"':'') +' points="'+rows.map(r=>x(r[0]).toFixed(1)+','+y(r[i]).toFixed(1)).join(' ')+'"/>'; let ticks=''; for(let i=0;i<=4;i++){const v=mx*i/4,yy=y(v); ticks+='<line x1='+L+' y1='+yy+' x2='+(W-R)+' y2='+yy+' stroke=#222 stroke-width=0.5></line>' +'<text x='+(L-6)+' y='+(yy+3)+' text-anchor=end>'+fmt(v)+'</text>';} const step=Math.ceil(rows.length/8); for(let t=1;t<=rows.length;t+=step) ticks+='<text x='+x(t)+' y='+(H-6)+' text-anchor=middle>'+t+'</text>'; return '<svg viewBox="0 0 '+W+' '+H+'" style="width:100%;height:auto">'+ticks +line(1,'#22c55e',2.2)+'</svg>'; } function render(){ for(const d of DEFS){if(d.length===2)continue;const id=d[0]; document.getElementById('v_'+id).textContent=(d[5]==='$'?full(P[id]):P[id]+d[5]);} const c=calc(); document.getElementById('st1').innerHTML= stat('Upfront payment',fmt(c.premium),P.p+'% of '+fmt(P.FH)+' lease value','var(--neon)') +stat('Rent, year 1',fmt(c.rent1)+'/yr',pct(c.yld,2)+' of value · +'+pct(c.G,1)+'/yr') +stat('Rent, final year',fmt(c.last)+'/yr','year '+P.T) +stat('Rent over term',fmt(c.nom),'land value at end '+fmt(c.mvEnd)); document.getElementById('chart').innerHTML=chart(c.rows); document.getElementById('note').innerHTML='Each deferred payment compounds three costs: the discount rate prices the waiting, the CX index keeps the money honest, and real land growth rides on top — the rent holds its share of the land by construction, so the review clause is baked into the schedule. Inflation is counted once (inside CX), never again inside land growth.'; } for(const d of DEFS){if(d.length===2)continue;const id=d[0]; document.getElementById('in_'+id).addEventListener('input',e=>{P[id]=parseFloat(e.target.value);render()});} render(); })();</script>

the model: V is the lease value at full prepayment. the upfront share p (30% minimum) is paid at signing; the remainder returns as a schedule growing at the combined rate G = (1+cx)(1+g)−1 and discounted at r: rent₁ = (1−p)·V / Σ (1+G)^(t−1)/(1+r)^t. the three costs compound without double counting — inflation lives inside CX once, g is real growth above it; the rent keeps its share of the land by construction.
