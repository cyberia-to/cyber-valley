---
tags: cyberia, cyber valley, strategy, tool
alias: annual leasehold, lease pricer, ground lease pricer, lease calculator
crystal-type: pattern
crystal-domain: cyberia
---
# annual leasehold

enter [[cyber valley]] with 30% down. the rest is a fixed yearly rent for 25 years — locked on day one, not renegotiated at market.

on bali, land often wants full cash or rides ~9% a year. that builds flips, not businesses. here the unpaid part grows at a fixed 3% real over Indonesian CPI — not tied to the [[century index]] — so the rest of the schedule is knowable on day one. so entrepreneurs can start building instead of scraping for a full cheque.

[[hak sewa]] with the estate: build, live, sublet. after build — [[hak pakai]] with KITAS/KITAP, or [[hak milik]] if you are WNI. full cash today? [[leasehold upfront]].

## price your deal

<div id="lcalc"></div>

<style>#lcalc{--bg:#000;--s1:#0a0a0a;--s2:#111;--ln:#222;--tx:#f0f0f0;--mut:#8b948c;--neon:#22c55e;--cyan:#06b6d4;--red:#ef4444;--amb:#eab308;background:transparent;color:var(--tx);font-family:var(--font-body,'Play',system-ui,sans-serif);border:none;box-shadow:none;box-sizing:border-box;width:min(73vw,calc(100vw - 2 * var(--sidebar-width, 220px) - 32px));max-width:calc(100vw - 2 * var(--sidebar-width, 220px) - 32px);position:relative;left:50%;transform:translateX(-50%);margin:28px 0;padding:0;} #lcalc .panel{background:var(--s1);border:1px solid var(--ln);border-radius:10px;padding:16px} #lcalc h4{grid-column:1/-1;font-family:var(--font-mono,'JetBrains Mono',monospace);font-size:11px;color:var(--neon);letter-spacing:2.5px;text-transform:uppercase;margin:10px 0 4px;text-shadow:0 0 10px rgba(34,197,94,.55)} #lcalc h4:first-child{margin-top:0} #lcalc .row{margin:0} #lcalc .row .top{display:flex;justify-content:space-between;align-items:baseline;margin-bottom:4px} #lcalc label{font-size:12.5px;color:var(--mut)} #lcalc .val{font-family:var(--font-mono,'JetBrains Mono',monospace);font-size:13px;color:var(--tx)} #lcalc input[type=range]{width:100%;accent-color:var(--neon);height:3px;cursor:pointer;margin:0} #lcalc .stat{background:var(--s2);border:1px solid var(--ln);border-radius:8px;padding:10px 12px;min-width:0} #lcalc .stat .l{font-size:11px;color:var(--mut);margin-bottom:4px;letter-spacing:.4px} #lcalc .stat .v{font-family:var(--font-mono,'JetBrains Mono',monospace);font-size:17px;font-weight:600;text-shadow:0 0 12px rgba(34,197,94,.25);word-break:break-word} #lcalc .stat .s{font-size:10.5px;color:var(--mut);margin-top:3px} #lcalc .hint{font-size:10.5px;color:var(--mut);margin-top:4px;line-height:1.4} #lcalc .note{font-size:12px;color:var(--mut);line-height:1.6;margin:10px 0 0} #lcalc svg text{font-family:var(--font-mono,'JetBrains Mono',monospace);font-size:10px;fill:var(--mut)} #lcalc table{width:100%;border-collapse:collapse;font-size:12.5px} #lcalc th{font-size:10.5px;color:var(--mut);text-transform:uppercase;letter-spacing:1px;text-align:left;padding:4px 10px;font-weight:400} #lcalc td{padding:5px 10px;border-top:1px solid var(--ln);font-family:var(--font-mono,'JetBrains Mono',monospace)} #lcalc tr.on td{color:var(--neon);background:rgba(34,197,94,.07)} #lcalc .dot{display:inline-block;width:7px;height:7px;border-radius:50%;margin-right:6px;vertical-align:1px} #lcalc input[data-driven]{pointer-events:none} #lcalc .tip{position:absolute;pointer-events:none;z-index:5;background:#111;border:1px solid #333;color:#f0f0f0;font-family:var(--font-mono,'JetBrains Mono',monospace);font-size:12px;padding:7px 10px;border-radius:6px;box-shadow:0 0 24px rgba(34,197,94,.2);white-space:nowrap;display:none} #lcalc .pt{cursor:crosshair} #lcalc .units{display:flex;flex-wrap:wrap;gap:6px;margin:14px 0 14px} #lcalc .unit{font-family:var(--font-mono,'JetBrains Mono',monospace);font-size:11px;letter-spacing:.5px;padding:6px 10px;border:1px solid #222;border-radius:999px;background:#0a0a0a;color:#8b948c;cursor:pointer} #lcalc .unit.on{color:#000;background:#22c55e;border-color:#22c55e;text-shadow:none} #lcalc .unit:hover{border-color:#22c55e;color:#f0f0f0} #lcalc .unit-note{font-size:10.5px;color:#8b948c;margin:0 0 16px;line-height:1.4}#lcalc .col-right .chart-panel{flex:1;display:flex;flex-direction:column;height:100%;min-height:100%;box-sizing:border-box}#lcalc .chart-wrap svg{flex:1;width:100%;min-height:300px;height:100%}#lcalc .col-right .chart-panel{flex:1;display:flex;flex-direction:column;height:100%;min-height:100%;box-sizing:border-box}#lcalc .chart-wrap svg{flex:1;width:100%;min-height:280px;height:100%}#lcalc .panel-controls .ctrl{flex:1}#lcalc .cols{display:grid;grid-template-columns:1fr 1fr;gap:20px 24px;align-items:stretch}#lcalc .col-left{display:flex;flex-direction:column;gap:14px;min-width:0;height:100%}#lcalc .col-right{display:flex;flex-direction:column;min-width:0;height:100%}#lcalc .panel-controls{flex:0 0 auto}#lcalc .stats{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px;flex:0 0 auto;align-content:start}#lcalc .chart-head{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:0 0 12px;flex:0 0 auto}#lcalc .col-right .chart-panel{flex:1;display:flex;flex-direction:column;height:100%;min-height:100%;box-sizing:border-box}#lcalc .chart-wrap{position:relative;flex:1;min-height:280px;display:flex;flex-direction:column}#lcalc .chart-wrap svg{flex:1;width:100%;min-height:280px;height:100%}#lcalc .ctrl{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px 14px}@media(max-width:768px){#lcalc{width:calc(100vw - 24px);max-width:calc(100vw - 24px);left:50%;transform:translateX(-50%);padding:0}#lcalc .cols{grid-template-columns:1fr;align-items:start}#lcalc .col-left,#lcalc .col-right{height:auto}#lcalc .ctrl{grid-template-columns:1fr}}#lcalc a.cx-link,#lcalc .hint a,#lcalc label a{color:var(--cyan);text-decoration:underline;text-underline-offset:2px}#lcalc a.cx-link:hover,#lcalc .hint a:hover,#lcalc label a:hover{color:var(--neon)}</style>

<script>(function(){ const el=id=>document.getElementById(id); const UNITS={ IDR:{id:"IDR",label:"IDR",perUsd:16000,kind:"fiat"}, USD:{id:"USD",label:"USD",perUsd:1,kind:"fiat"}, CNY:{id:"CNY",label:"CNY",perUsd:7.25,kind:"fiat"}, GOLD:{id:"GOLD",label:"GOLD",perUsd:0.01,kind:"metal"}, BTC:{id:"BTC",label:"BTC",perUsd:0.00001,kind:"crypto"}, ETH:{id:"ETH",label:"ETH",perUsd:1/3500,kind:"crypto"} }; const UNIT_ORDER=["IDR","USD","CNY","GOLD","BTC","ETH"]; const BASE_MONEY=0.5; const FINANCING_SPREAD=5; const VALUE_MIN_USD=20000; const VALUE_MAX_USD=2000000; const state={valueUsd:125000,term:25,upfront:30,money:4,inflation:6,land:3,unit:"IDR",ratesLive:false,ratesAt:""}; const financingRate=upfront=>Math.round((BASE_MONEY+FINANCING_SPREAD*(1-upfront/100))*10)/10; function unit(){return UNITS[state.unit]} function toDisplay(usdAmt){return usdAmt*unit().perUsd} function fromDisplay(disp){return disp/unit().perUsd}function roundIdr(x){const n=Math.abs(x);let step;if(n>=1e9)step=1e8;else if(n>=1e8)step=1e7;else if(n>=1e7)step=1e6;else if(n>=1e6)step=1e5;else if(n>=1e3)step=1e3;else step=1;const r=Math.round(n/step)*step;return x<0?-r:r;} function fmt(usdAmt){ const u=unit(); const x=toDisplay(usdAmt); if(u.id==="IDR"){const n=roundIdr(x);const a=Math.abs(n);const sign=n<0?"-":"";if(a>=1e12){const v=a/1e12;return sign+"Rp "+(Number.isInteger(v)?v:v.toFixed(1))+"T";}if(a>=1e9){const v=a/1e9;return sign+"Rp "+(Number.isInteger(v)?v:v.toFixed(1))+"B";}if(a>=1e6){const v=a/1e6;return sign+"Rp "+(Number.isInteger(v)?v:Math.round(v))+"M";}return sign+"Rp "+Math.round(a).toLocaleString("en-US");} if(u.id==="USD"){ if(Math.abs(x)>=1000) return "\u0024"+(x/1000).toFixed(Math.abs(x)>=100000?0:1)+"k"; return "\u0024"+Math.round(x); } if(u.id==="CNY"){ if(Math.abs(x)>=1000) return "¥"+(x/1000).toFixed(Math.abs(x)>=100000?0:1)+"k"; return "¥"+Math.round(x); } if(u.id==="GOLD"){ if(Math.abs(x)>=1000) return (x/1000).toFixed(2)+" kg Au"; return x.toFixed(1)+" g Au"; } if(u.id==="BTC"){ if(Math.abs(x)>=1) return "₿"+x.toFixed(3); if(Math.abs(x)>=0.01) return "₿"+x.toFixed(4); return "₿"+x.toFixed(6); } if(u.id==="ETH"){ if(Math.abs(x)>=1) return "Ξ"+x.toFixed(2); return "Ξ"+x.toFixed(4); } return String(x); } function fmtFull(usdAmt){ const u=unit(); const x=toDisplay(usdAmt); if(u.id==="IDR") return "Rp "+Math.round(x).toLocaleString("en-US"); if(u.id==="USD") return "\u0024"+Math.round(x).toLocaleString("en-US"); if(u.id==="CNY") return "¥"+Math.round(x).toLocaleString("en-US"); if(u.id==="GOLD") return Math.abs(x)>=1000?(x/1000).toFixed(3)+" kg Au":x.toFixed(2)+" g Au"; if(u.id==="BTC") return "₿"+x.toFixed(6); if(u.id==="ETH") return "Ξ"+x.toFixed(4); return String(x); } const percent=(v,d=1)=>v.toFixed(d)+"%"; function valueSliderRange(){ const u=unit(); const minD=toDisplay(VALUE_MIN_USD); const maxD=toDisplay(VALUE_MAX_USD); let step; if(u.id==="IDR")step=100000000; else if(u.id==="USD") step=10000; else if(u.id==="CNY") step=50000; else if(u.id==="GOLD") step=50; else if(u.id==="BTC") step=0.01; else step=0.1; return {min:minD,max:maxD,step:step,value:toDisplay(state.valueUsd)}; } const CONTROLS=[ {section:"Your plot"}, {id:"value",label:"Plot price",isValue:true,hint:"100% at signing; switch unit above"}, {id:"upfront",label:"Deposit at signing",min:30,max:90,step:10,suffix:"%",hint:"30-90% of full price; the rest becomes yearly rent"}, {section:"How rent grows"}, {id:"money",label:"Fee for paying later",min:0,max:5,step:0.1,suffix:"%",tone:"cyan",nature:"depends on deposit",drivenByUpfront:true,hint:"see <a class=cx-link href=#b-fee-by-deposit-size>appendix B</a>"}, {id:"land",label:"Land growth",min:0,max:6,step:0.5,suffix:"%",tone:"cyan",nature:"fixed",hint:"locked at 3% real on top of <a class=cx-link href=century-index>CX</a>",locked:true}, {id:"inflation",label:"<a class=cx-link href=century-index>Century index (CX)</a>",min:0,max:12,step:0.5,suffix:"%",tone:"amb",nature:"expectation only",hint:"real invoices follow the live index"} ]; function model(s){ const deposit=s.upfront/100*s.valueUsd; const financed=s.valueUsd-deposit; const discount=s.money/100; const growth=(1+s.inflation/100)*(1+s.land/100)-1; let annuity=0; for(let t=1;t<=s.term;t++)annuity+=Math.pow(1+growth,t-1)/Math.pow(1+discount,t); const firstRent=financed/annuity; const rents=[]; let rent=firstRent,rentSum=0; for(let t=1;t<=s.term;t++){ if(t>1)rent*=1+growth; rents.push(rent); rentSum+=rent; } const totalCash=deposit+rentSum; const vsFull=totalCash-s.valueUsd; const vsFullPct=s.valueUsd>0?vsFull/s.valueUsd*100:0; return{deposit,financed,firstRent,lastRent:rent,rentSum,rents,totalCash,vsFull,vsFullPct,rate:discount*100,growthPct:growth*100}; } function card(label,value,caption,color){ return "<div class=stat><div class=l>"+label+"</div><div class=v"+(color?" style=color:"+color:"")+">"+value+"</div>"+(caption?"<div class=s>"+caption+"</div>":"")+"</div>"; } function chart(rents){ const W=960,H=420,left=58,right=16,top=16,bottom=30; const plotW=W-left-right,plotH=H-top-bottom; const peak=Math.max(...rents,1)*1.06; const x=t=>left+plotW*(t-1)/Math.max(rents.length-1,1); const y=v=>top+plotH*(1-v/peak); let grid=""; for(let i=0;i<=4;i++){ const v=peak*i/4,yy=y(v); grid+="<line x1="+left+" y1="+yy+" x2="+(W-right)+" y2="+yy+' stroke="#222" stroke-width="0.5"></line>'; grid+="<text x="+(left-6)+" y="+(yy+3)+' text-anchor="end" fill="#8b948c" font-size="10">'+fmt(v)+"</text>"; } const step=Math.max(1,Math.ceil(rents.length/8)); for(let t=1;t<=rents.length;t+=step){ grid+="<text x="+x(t)+" y="+(H-8)+' text-anchor="middle" fill="#8b948c" font-size="10">'+t+"</text>"; } const points=rents.map((v,i)=>x(i+1).toFixed(1)+","+y(v).toFixed(1)).join(" "); let dots=""; rents.forEach((v,i)=>{ const year=i+1; const cx=x(year).toFixed(1), cy=y(v).toFixed(1); const label="year "+year+": "+fmtFull(v)+"/yr"; dots+='<circle class="pt" data-year="'+year+'" data-rent="'+v+'" cx="'+cx+'" cy="'+cy+'" r="11" fill="transparent"></circle>'; dots+='<circle class="pt" data-year="'+year+'" data-rent="'+v+'" cx="'+cx+'" cy="'+cy+'" r="4.5" fill="#0a0a0a" stroke="#22c55e" stroke-width="2"><title>'+label+"</title></circle>"; }); return '<svg id="chart-svg" viewBox="0 0 '+W+" "+H+'" style="width:100%;height:auto;display:block">'+grid+'<polyline fill="none" stroke="#22c55e" stroke-width="2.2" points="'+points+'"></polyline>'+dots+"</svg>"; } function bindChartTip(){ const wrap=el("chart"); if(!wrap)return; let tip=el("chart-tip"); if(!tip){ tip=document.createElement("div"); tip.id="chart-tip"; tip.className="tip"; wrap.appendChild(tip); } const svg=el("chart-svg"); if(!svg)return; const show=(circle,year,rent)=>{ tip.style.display="block"; tip.textContent="year "+year+" · "+fmtFull(rent)+"/yr"; const wrapRect=wrap.getBoundingClientRect(); const pt=circle.getBoundingClientRect(); const tipW=tip.offsetWidth||160; const tipH=tip.offsetHeight||28; const cx=pt.left-wrapRect.left+pt.width/2; const cy=pt.top-wrapRect.top+pt.height/2; let left=cx+10; let top=cy-tipH/2; if(left+tipW>wrapRect.width-4) left=cx-tipW-10; if(left<4) left=4; if(top<4) top=4; if(top+tipH>wrapRect.height-4) top=Math.max(4,wrapRect.height-tipH-4); tip.style.left=left+"px"; tip.style.top=top+"px"; }; const hide=()=>{tip.style.display="none"}; svg.querySelectorAll("circle.pt").forEach(c=>{ const year=+c.getAttribute("data-year"); const rent=+c.getAttribute("data-rent"); c.addEventListener("mouseenter",()=>show(c,year,rent)); c.addEventListener("mousemove",()=>show(c,year,rent)); c.addEventListener("mouseleave",hide); });} function syncMoneyFromUpfront(){ state.money=financingRate(state.upfront); const slider=el("in_money"); if(slider)slider.value=state.money; } function unitsHtml(){ return UNIT_ORDER.map(id=>{ const u=UNITS[id]; return '<button type="button" class="unit'+(state.unit===id?" on":"")+'" data-unit="'+id+'">'+u.label+"</button>"; }).join(""); } function unitNote(){const u=unit();const src=state.ratesLive?("live "+state.ratesAt):"fallback (offline)";const idr=Math.round(UNITS.IDR.perUsd).toLocaleString("en-US");const cny=Number(UNITS.CNY.perUsd).toFixed(2);const goldPerG=(1/UNITS.GOLD.perUsd);const btc=Math.round(1/UNITS.BTC.perUsd).toLocaleString("en-US");const eth=Math.round(1/UNITS.ETH.perUsd).toLocaleString("en-US");if(u.id==="IDR") return "IDR · 1 USD = Rp "+idr+" · "+src;if(u.id==="USD") return "USD · base unit of the model · "+src;if(u.id==="CNY") return "CNY · 1 USD = ¥"+cny+" · "+src;if(u.id==="GOLD") return "GOLD · ~USD "+goldPerG.toFixed(0)+"/g Au · "+src;if(u.id==="BTC") return "BTC · ~USD "+btc+"/BTC · "+src;if(u.id==="ETH") return "ETH · ~USD "+eth+"/ETH · "+src;return src;} function render(){ syncMoneyFromUpfront(); const unitsEl=el("units"); if(unitsEl){ unitsEl.innerHTML=unitsHtml(); unitsEl.querySelectorAll(".unit").forEach(btn=>{ btn.addEventListener("click",()=>{state.unit=btn.getAttribute("data-unit");buildControls(true);render();}); }); } const noteEl=el("unit-note"); if(noteEl) noteEl.textContent=unitNote(); const vr=valueSliderRange(); const inv=el("in_value"); if(inv){ inv.min=vr.min; inv.max=vr.max; inv.step=vr.step; inv.value=vr.value; } const vv=el("v_value"); if(vv) vv.textContent=fmtFull(state.valueUsd); for(const c of CONTROLS){ if(!c.id||c.isValue) continue; const node=el("v_"+c.id); if(!node) continue; if(c.id==="money") node.textContent=percent(state.money,1); else node.textContent=state[c.id]+c.suffix; } const m=model(state); const extraColor=m.vsFull>0?"var(--amb)":"var(--neon)"; const extraLabel=m.vsFull>=0?"extra vs full pay now":"cheaper than full pay now"; const extraVal=(m.vsFull>=0?"+":"")+fmt(m.vsFull)+" ("+(m.vsFull>=0?"+":"")+percent(m.vsFullPct,1)+")"; el("st1").innerHTML=card("Pay now (deposit)",fmt(m.deposit),state.upfront+"% of "+fmt(state.valueUsd),"var(--neon)")+card("Yearly rents (all years)",fmt(m.rentSum),"only the unpaid "+fmt(m.financed)+" — deposit not included")+card("Total you pay",fmt(m.totalCash),"deposit + all yearly rents")+card(extraLabel,extraVal,"full price today would be "+fmt(state.valueUsd),extraColor);const ch=el("st-chart");if(ch) ch.innerHTML=card("First year rent",fmt(m.firstRent)+"/yr","year 1")+card("Last year rent",fmt(m.lastRent)+"/yr","year "+state.term); el("chart").innerHTML=chart(m.rents); bindChartTip(); } function buildControls(bind){ let controlsHtml=""; for(const c of CONTROLS){ if(c.section){controlsHtml+="<h4>"+c.section+"</h4>";continue} const tone=c.tone?"var(--"+c.tone+")":""; const label=(tone?'<i class="dot" style="background:'+tone+'"></i>':"")+c.label; const valStyle=tone?' style="color:'+tone+'"':""; const attrs=(c.drivenByUpfront||c.locked)?' data-driven="1" style="accent-color:'+(tone||"var(--neon)")+'"':(tone?' style="accent-color:'+tone+'"':""); let min=c.min,max=c.max,step=c.step,val=state[c.id]; if(c.isValue){ const vr=valueSliderRange(); min=vr.min; max=vr.max; step=vr.step; val=vr.value; } controlsHtml+='<div class="row"><div class="top"><label>'+label+"</label><span class=val id=v_"+c.id+valStyle+"></span></div>"+ '<input type="range" id="in_'+c.id+'" min="'+min+'" max="'+max+'" step="'+step+'" value="'+val+'"'+attrs+">"+ (c.hint?'<div class="hint">'+(c.nature?'<span style="color:'+tone+'">'+c.nature+"</span> — ":"")+c.hint+"</div>":"")+ "</div>"; } el("controls").innerHTML=controlsHtml; if(bind){ for(const c of CONTROLS){ if(!c.id||(c.drivenByUpfront||c.locked)) continue; const input=el("in_"+c.id); if(!input) continue; input.oninput=e=>{ if(c.isValue){ state.valueUsd=fromDisplay(parseFloat(e.target.value));if(state.unit==="IDR"){const idr=Math.round(toDisplay(state.valueUsd)/1e8)*1e8;state.valueUsd=idr/UNITS.IDR.perUsd;} if(state.valueUsd<VALUE_MIN_USD) state.valueUsd=VALUE_MIN_USD; if(state.valueUsd>VALUE_MAX_USD) state.valueUsd=VALUE_MAX_USD; } else { state[c.id]=parseFloat(e.target.value); } render(); }; } } } function applyRates(idrPerUsd,cnyPerUsd,btcUsd,ethUsd,goldUsdPerOz,when){if(idrPerUsd>0) UNITS.IDR.perUsd=idrPerUsd;if(cnyPerUsd>0) UNITS.CNY.perUsd=cnyPerUsd;if(btcUsd>0) UNITS.BTC.perUsd=1/btcUsd;if(ethUsd>0) UNITS.ETH.perUsd=1/ethUsd;if(goldUsdPerOz>0) UNITS.GOLD.perUsd=31.1034768/goldUsdPerOz;state.ratesLive=true;state.ratesAt=when||"now";if(state.unit==="IDR"){const targetIdr=2000000000;state.valueUsd=targetIdr/UNITS.IDR.perUsd;}buildControls(true);render();}function loadRates(){const fiat=fetch("https://open.er-api.com/v6/latest/USD").then(function(r){return r.json();});const crypto=fetch("https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum,pax-gold&vs_currencies=usd").then(function(r){return r.json();});Promise.all([fiat,crypto]).then(function(pair){const f=pair[0],c=pair[1];const idr=f&&f.rates&&f.rates.IDR;const cny=f&&f.rates&&f.rates.CNY;const btc=c&&c.bitcoin&&c.bitcoin.usd;const eth=c&&c.ethereum&&c.ethereum.usd;const goldOz=c&&c["pax-gold"]&&c["pax-gold"].usd;const when=(f&&f.time_last_update_utc)?String(f.time_last_update_utc).replace(" +0000"," UTC"):"live";applyRates(idr,cny,btc,eth,goldOz,when);}).catch(function(){state.ratesLive=false;state.ratesAt="fallback";const noteEl=el("unit-note");if(noteEl) noteEl.textContent=unitNote()+" · rate fetch failed, using fallbacks";});}el("lcalc").innerHTML='<div class="cols"><div class="col-left"><div class="panel panel-controls"><h4>Display unit</h4><div id="units" class="units"></div><p id="unit-note" class="unit-note"></p><div id="controls" class="ctrl"></div></div><div class="stats" id="st1"></div></div><div class="col-right"><div class="panel chart-panel"><div class="chart-head stats" id="st-chart"></div><h4>Yearly rent chart</h4><div id="chart" class="chart-wrap"></div></div></div></div>';buildControls(true);render();loadRates();})();</script>

## appendices

### A · reading the numbers

| term | meaning |
|---|---|
| pay now | deposit only |
| yearly rents | all annual payments — no deposit |
| total you pay | deposit + rents |
| extra vs full pay | how much more (or less) than 100% cash today |
| first year rent | instalment in year 1 |
| last year rent | instalment in the final year |
| chart | hover any year for the exact amount |

### B · fee by deposit size

bigger deposit → thinner fee. rent path still fixed at signing.

| deposit | 30% | 40% | 50% | 60% | 70% | 80% | 90% |
|---|---:|---:|---:|---:|---:|---:|---:|
| fee | 4.0% | 3.5% | 3.0% | 2.5% | 2.0% | 1.5% | 1.0% |

`fee = 0.5% + 5% × (1 − deposit)`. 100% prepaid → [[leasehold upfront]].

### C · display units

IDR · USD · CNY · GOLD · BTC · ETH. live on load (open.er-api + CoinGecko; GOLD via PAX Gold → g). offline → fallbacks. switching unit does not change the deal.

### D · legal form

[[hak sewa]] now; after build → [[hak pakai]] (KITAS/KITAP) or [[hak milik]] (WNI). skeleton: [[land rights agreement]].
